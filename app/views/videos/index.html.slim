.col.m12.l11.offset-l1.offset-m1.offset-s1
  h2.h4
    | 新着人気グラビア動画
  - @videos.each do |video|
    .col.m4.l3
      .card.small
        .card-image.waves-effect.waves-block.waves-light
          = link_to video do
            = image_tag("#{video.thumbnail}", alt: video.title, class: 'activator')
        .card-content
          = truncate(video.title, long: 50)
        .card-action
          = link_to '動画を見る', video

  = paginate(@videos)
