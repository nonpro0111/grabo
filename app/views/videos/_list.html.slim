- videos.each do |video|
  .col.s12.m4.l3
    .card
      .card-image.waves-effect.waves-block.waves-light
        = link_to video do
          = image_tag("#{video.thumbnail}", alt: video.title, class: 'activator')
      .card-content.small-content
        = link_to(truncate(clean_text(video.title), long:25), video)

= paginate(videos)

